// Define the URL shortener variable
let shortener;

/**
 * TODO: Create a function to initialize the URL shortener
 * (hint: use the UrlShortener class and pass the current origin as the base URL)
 */
function initializeShortener() {
    // Initialize the shortener with proper error handling
}

/**
 * TODO: Create a function to handle the URL shortening process
 * (hint: get input value, use the shortener, and update the DOM to show results)
 */
function shortenURL() {
    // Check if shortener is initialized
    
    // Get required DOM elements
    
    // Try to shorten the URL and handle the result
    // - Hide any previous errors
    // - Display the shortened URL
    // - Make the copy button visible
    
    // Catch and display any errors
}

/**
 * TODO: Create a function to copy the shortened URL to clipboard
 * (hint: use the navigator.clipboard API and provide user feedback)
 */
function copyToClipboard() {
    // Check if application is ready
    
    // Get the shortened URL text
    
    // Copy to clipboard and handle success/failure
    // - Provide visual feedback when copied
}

// TODO: Initialize the shortener when the page loads
// (hint: use the DOMContentLoaded event)


// TODO: Make functions available to the HTML elements
// (hint: add to the window object)
